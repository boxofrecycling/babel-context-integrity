# Distribution digests — v0.1.0

Built locally with `python -m build`.

```
f0e731532f2d1f681e92a1eb467f30f1568ab73761a1970be1c77e5b337c254f  babel_context_integrity-0.1.0-py3-none-any.whl
117f9b76993e74720866ec314dc8bb2864143e56d102f2e778d4bc7fbfdcc41a  babel_context_integrity-0.1.0.tar.gz
```

Sizes:
```
babel_context_integrity-0.1.0-py3-none-any.whl          53632 bytes
babel_context_integrity-0.1.0.tar.gz                   127962 bytes
```

## Reproducibility

The **wheel is bit-reproducible**: rebuilding from a fresh clone of the
same commit, on the same Python, produces a byte-identical file.

The **sdist is content-reproducible but not bit-reproducible**: its
extracted tree is identical across builds, while the gzip container
embeds a timestamp, so the outer digest changes. That is a property of
the archive format, not of the contents.

`release/DIGESTS.txt` is excluded from the sdist: an archive cannot
contain its own hash.
